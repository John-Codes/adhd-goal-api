Convert the existing CRUD operations for quarterly and monthly goals into a unified MongoDB collection. Use a single  collection where each document represents one goal.
 Include fields to distinguish between quarterly and monthly goals (e.g.,  or ). Each goal document should reference the user via a  or  field, rather than using the email as the document .
 This approach supports multiple users, avoids document size limits, and aligns with MongoDB’s flexible schema design
Q

{
  "_id": ObjectId("..."),              // Unique goal ID (MongoDB default)
  "userId": ObjectId("..."),           // Reference to the user (from Users collection)
  "goalType": "monthly",               // "monthly" or "quarterly"
  "goalText": "Increase traffic by 20%",
  "goalTitle":"Optimize SEO strategy",
  "priority": "high",                  // "high", "medium", "low"
  "status": "active",                  // "active", "completed", "archived", "deleted"
  "tags": ["growth", "marketing", "Q1"], // Optional categorization
  "createdAt": ISODate("2025-11-10T00:00:00Z"),
  "updatedAt": ISODate("2025-11-10T00:00:00Z"),
  "deadline": ISODate("2025-12-31T00:00:00Z"),
  "isDeleted": false                   // Soft delete flag
}



# Build (15 seconds)
docker build -t goals-api:latest .

# Run container
docker run -d -p 8000:8000 --name goals-api goals-api:latest

# Test
curl http://localhost:8000/api/health
